# Expo + React Native do not handle pnpm's nested symlink layout well.
# Hoist dependencies to a flat node_modules so Metro/native modules resolve.
# See https://docs.expo.dev/guides/monorepos/#pnpm
node-linker=hoisted
